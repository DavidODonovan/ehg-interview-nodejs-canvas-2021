import styled from 'styled-components';

export const Layout=styled.div`
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: var(--bg);
`;

export const Container=styled.div`
  padding-top: 3.5em;
  max-width: 1066px;
  margin: auto;
  overflow-y: scroll;
  height: 100%;
  display: flex;
`;
